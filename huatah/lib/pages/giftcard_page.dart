import "package:flutter/material.dart";

class GiftcardPage extends StatelessWidget {
  const GiftcardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Giftcard Page"),
    );
  }
}
